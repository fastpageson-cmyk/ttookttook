node_modules/
dist/
.DS_Store
.vite/

.claude/

# 학교 교육자료 원본(docx) — 레포가 public이라 원본은 제외하고
# 앱용으로 정제한 원고만 docs/콘텐츠/에 포함한다
교육 자료/

# API 키 — 절대 커밋 금지. DART 오픈API 키는 빌드 타임(데이터 생성 스크립트)에서만 쓴다.
# 프론트엔드는 정적 SPA라 번들에 넣으면 그대로 노출되므로 VITE_ 접두사로 쓰지 말 것.
.env
.env.*
!.env.example

