---
tags: [똑똑, tasks]
---

# 작업 체크리스트 (Decomposition)

> 이 문서가 "똑똑" 프로젝트의 할 일 목록 역할을 한다(2026-07-19 신설).
> Claude Code가 실제로 짤 작업 단위. 체크박스 하나 = 한 세션에서 끝낼 수 있는 크기로 쪼갠다.
> 스코프·완료기준의 원문 근거는 [[빌드_브리프_클로드코드용]] · [[화면기획서]]. 로컬 작업 폴더는
> `~/Desktop/post/커리어/project/똑똑/` (이 문서들의 사본이 `docs/`에 있음 — 갱신 시 함께 동기화).
> 프로젝트명 확정: 구명 "뚝뚝" → **"똑똑"** (2026-07-19). 슬로건: **"먼저 잃고, 똑똑해집니다"**

## Phase 0 — 스캐폴딩 (2026-07-19 완료)
- [x] 프로젝트 초기화 — **Vite 6 + 순수 JS(ES 모듈), React 미사용** (형제 프로젝트 컨벤션과 동일 계열). `npm run dev` 포트 5174
- [x] `docs/콘텐츠/mock-data/*.json` → `src/data/`로 반입, Vite JSON import로 로딩
- [x] 전체 state 구조를 [[데이터모델]]의 TypeScript 인터페이스 기준으로 세팅 (`src/state.js`, localStorage 저장 — 새로고침 시 유지 확정)

## Phase 1 — MVP 구현 (Must = 0단계 + 1주차) — 2026-07-19 완료
화면 번호는 [[화면기획서]] 3-1~3-12 기준. 전부 실제 브라우저 E2E로 검증(콘솔 에러 0).
- [x] 1. 스플래시/온보딩 — 새 슬로건 + 카드 3장 스와이프 + 재방문 시 생략
- [x] 2. 시작 화면(닉네임 입력 — 회원가입 없음)
- [x] 3. 사전 진단 20문항 — 진행률·이전 이동·중도이탈 임시저장(재진입 시 이어서)
- [x] 4. 진단 결과 — 점수 카운트업 + 또래 평균 62.6점 비교 막대 애니메이션
- [x] 5. 0단계 블라인드 자유투자 — 종목 8+지수ETF, 520주, 재생/빨리감기/+4주, 매수·매도 바텀시트(수수료 0.2% 자동 계산), 보유 표시, 자동/수동 종료
- [x] 6. 0단계 종료 리포트 — 수익률 카운트업·MDD·매매횟수·수수료·지수ETF 주간적립(DCA) 비교선 그래프
- [x] 7. AI 매매 실수 리포트 — **규칙 기반** 6종 룰(최대손실 종목/팔고 오른 매매/잦은 매매·수수료/몰빵/벤치마크 비교/무매매)에서 심각도 상위 3건. `generateMistakeReport()` 시그니처 유지로 추후 LLM 교체 가능
- [x] 8. 홈/대시보드 — 다음 액션 카드 + 진도 6단계 + 커리큘럼(2~6주차 잠김)
- [x] 9. 1주차 강의 카드 10개 — 원고 그대로, SVG 인포그래픽 2종 포함, 열람 추적
- [x] 10. 실습 3종 — ①0단계 복기(금리 국면 밴드 클릭 + 내 매매 오버레이) ②금리 인하기 리플레이(체크포인트 질문 2회+균형 해설) ③한미 금리차 슬라이더 위젯(예측→정답 공개)
- [x] 11. 확인 퀴즈 5문항 — 통과 기준 4/5, 오답 시 해당 강의 카드로 점프 링크, 재도전
- [x] 12. 마이페이지 — 진행 이력 + 전체 초기화
- [x] 전체 흐름 E2E: 닉네임 → 진단 → 0단계 → 리포트 → AI 리포트 → 강의 → 실습 3종 → 퀴즈 통과 → 홈 "1주차 완료 🎉"까지 한 사이트 안에서 끊김 없이 동작 확인 (2026-07-19, 콘솔 에러 0, `npm run build` 통과)

## 디자인 디벨롭 백로그 — 2026-07-19 전부 반영
- [x] **숫자 카운트업 모션** — `countUp()` 공용 컴포넌트. 진단 점수·0단계 수익률에 적용
- [x] **결과 공유 카드** — 캔버스로 성적표 PNG 생성·저장(`성적표 이미지로 저장하기` 버튼, 외부 라이브러리 없음). *자동 테스트에서는 파일 다운로드 특성상 코드 경로만 검증 — 사용자 실기기 클릭 확인 1회 권장*
- [x] **선택지 정오답 마이크로 피드백** — 체크/X 아이콘 + 스프링 팝 + 오답 셰이크. 퀴즈·실습2 체크포인트·실습3 정답 공개에 공통 적용
- [x] **스플래시 카피 교체** — "먼저 잃고, 똑똑해집니다" 적용(스플래시·홈·공유 카드·퀴즈 통과 화면)

## 배포 & 데스크톱 (2026-07-19 완료)
- [x] **배포** — GitHub `fastpageson-cmyk/ttookttook`(public) + Vercel `ttookttook` 자동배포. 프로덕션 **https://ttookttook.vercel.app**. (경위: 코덱스가 다른 환경에서 같은 과제를 병렬로 React 버전으로 먼저 배포해뒀던 걸, 우리 순수JS 버전을 그 히스토리 위에 얹어 교체)
- [x] **데스크톱 레이아웃(≥1024px)** — 스플래시 히어로 2컬럼 / 0단계 시뮬 트레이딩 2컬럼 / 상단 네비 / 읽기형 680px 중앙정렬 / 바텀시트→중앙 다이얼로그. 모바일 단일컬럼 순서 유지. **누락 원인**: 이번 세션 순수JS 재구현 때 "모바일 우선" 문구만 보고 데스크톱 처리를 안 넣었음(코덱스판엔 있었으나 우리판이 덮어씀). 실측 검증 완료.

## 코덱스판 추가분·미구현분 반영 (2026-07-19 완료)
- [x] **사전진단 오답 풀이 + 영역별 이해도** — 진단 결과 화면에 카테고리별 정답률 막대(강=초록/중=주황/약=빨강) + 틀린 문항마다 [내 답/정답/오답풀이] 카드. `content.js` DIAGNOSIS 20문항 explain + `categoryBreakdown()`.
- [x] **점수별 페르소나 4단계** — 진단 결과 상단 페르소나 카드(이모지·유형명·설명·학습 가이드). `content.js` PERSONAS/`personaForScore()`.
- [x] **0단계 파산 처리** — 총자산이 시작 자금의 20%(=-80%) 미만이면 파산 모달(⚠️)→종료, 리포트에 💥 파산 배너. `BANKRUPT_RATIO=0.20`(sim-core). **임계값 근거**: 목업 데이터상 단일 종목 all-in 최악 손실이 B바이오 -84%/D플랫폼 -81%라 10%는 절대 도달 불가(죽은 코드) → 20%로 현실화. 스펙 3-5의 '잔고 0' 완전파산은 상장폐지 데이터가 있어야 성립(데이터 확보 시 하향). 실측 검증: 파산 발동·배너·AI 리포트 연결 확인.

## 2~6주차 전개 + 주차별 미니 모의투자 (2026-07-21 완료)
학교 교육자료(`교육 자료/` docx 6개)를 원천으로 2~6주차를 구현하고, **모든 주차 실습의 마지막에
미니 모의투자**를 붙였다. 각 미니 모의투자는 **그 주차에서 배운 개념을 실제로 조작**하게 만든다.

- [x] **원고 정제** — `docs/콘텐츠/2~6주차_*.md` 5종 신설. 교육자료 docx가 원천, 이 원고가 앱의 원본,
      `src/weeks/weekN.js`가 사본. **원본 docx는 레포 미포함**(public이라 `.gitignore` 처리)
- [x] **state 다주차화** — `S.week1` 단일 구조 → `S.weeks[1..6]`. 기존 localStorage 진행 상황은
      `migrate()`가 보존 승격. 주차별 `{cardsViewed, practices, miniSim, quiz}`
- [x] **화면 일반화** — `screens/week.js`에 강의/실습허브/퀴즈를 `{week}` 파라미터로 통합.
      홈 커리큘럼 6주차 표시 + 잠금(1주차는 0단계 종료, 2~6주차는 직전 주차 퀴즈 통과 시 해제)
- [x] **미니 모의투자 엔진** — `src/mini-sim.js`. 공용 셸(브리핑→설정→진행→결과) + 주차별 기전 주입.
      1주차만 기존 실습 3종 뒤에 4번째로 붙고, 2~6주차는 미니 모의투자가 실습 그 자체
- [x] **주차별 기전** (전부 브라우저 실측 검증, 콘솔 에러 0)
  - 1주차 · **금리 국면 읽기** — 국면 3택 + 주식/예금 비중 → 26주. 긴축기 주식 +1.2% vs 완화전환기 +17.8%
  - 2주차 · **24개월 시드머니** — 저축률·수단 선택. 적금 실효수익률(회차별 이자 + 세금 15.4%) 실제 계산 →
    연 4% 적금이 24개월 원금 대비 **+3.53%**. 금리 2배(+76만) vs 저축액 2배(+2,236만) 대비로 카드 4 증명
  - 3주차 · **레버리지 당겨보기** — 배수 0/0.5/1/2배 + 상환방식 선택, 대출 연 6%, **담보비율 25% 미만 시
    반대매매**. 2배·만기일시·하락장이면 **22주차에 강제 청산(-44.8%)**, 2배 상승장은 +155.6%(시장 +56.5%)
  - 4주차 · **폭락장 통과** — 주식/채권/금/현금 배분 + 리밸런싱 규칙 3택 → 104주.
    분산+리밸런싱 MDD 14.1% vs 주식100% MDD 23.7%. 결과 화면은 수익률보다 **MDD를 먼저** 보여줌
  - 5주차 · **숫자를 보고 고르기** — PER/PBR/영업이익률/업종평균이 붙은 8종목 중 3택 + 뉴스 4건
    (리딩방·유튜브=소음 / DART 실적·채무상환 유상증자=신호). **C케미칼을 저PER 가치함정으로 배치**
  - 6주차 · **같은 수익, 다른 잔고** — 일반/ISA/연금 중 선택 후 '목돈 필요' 이벤트.
    세전 순이익 250만 동일인데 세후 일반 1,196만 / ISA 1,250만 / 연금 중도해지 1,176만
- [x] 빌드 통과 · 데스크톱(1280) 가로 오버플로 0 · 모바일(375) 오버플로 0 실측

### 검증 중 잡은 버그 (수정 완료)
- `prac2`의 지역변수 `week`가 import한 `week()` 함수를 가려 실습2 완료 시 TypeError → `weekState` 별칭
- 3주차 결과 차트가 원화 원본값을 넘겨 y축이 `777524573%`로 깨짐 → `EQUITY0`로 정규화
- 3주차 중도 종료 시 비교군·시장수익률이 52주 전체 기준이라 코칭 문구가 어긋남 → `endIndex` 기준 정렬
- 3주차 1주차 즉시 종료(`exitAt=0`)가 루프(`i=1`부터)에 안 걸려 전체 실행 → 루프 전 조기 반환
- 6주차 비교표에서 '연금 유지'가 최고 막대로 보여 오해 유발 → `인출 불가` 라벨 + 비교 기준에서 제외

### 사용자 실기기 QA 피드백 (2026-07-21, 전부 수정 완료)
사용자 실기기 확인 결과 **정상 작동, 공유 카드 PNG 다운로드도 정상**. 아래 3건만 지적되어 수정했다.
- [x] **퀴즈 미통과 화면에 `null` 노출** — `parent.append(cond ? h(...) : null)`이 DOM에서 문자열
      "null"로 렌더됨. `ui.js`에 `appendKids()` 추가하고 위험 지점 3곳(퀴즈 결과, 3주차 설정/진행)에 적용.
      정적 스캔으로 전 소스에서 같은 패턴을 찾아 일괄 처리 — **`h()` 자식은 안전, `append()` 직접 호출만 위험**
- [x] **실습 허브에서 '지난 결과' 줄이 카드와 겹침** — 인라인 `margin-top:-6px` 제거,
      `.ms-last` 클래스로 분리(현재 카드 하단과 +10px 여백 실측 확인)
- [x] **종목 카드의 '영업이익률'이 "영업이익/률"로 줄바꿈** — `.ms-st-figs` 4열 → 2×2열로 변경,
      라벨 `white-space: nowrap`. 모바일 375px에서 한 줄 표시 실측 확인

## 실제 시장 데이터 전환 (2026-07-22 완료)
목업 합성 데이터를 실제 과거 시세로 교체했다. 생성 스크립트는 `docs/콘텐츠/mock-data/`에 있다.

- [x] **stocks.json** — 국내 8종목 + KOSPI, 520주(2016-07~2026-07). 출처 FinanceDataReader(KRX 공개시세, 수정주가).
      종목: 삼성전자·SK하이닉스·NAVER·카카오·현대차·KT&G·아모레퍼시픽·한국전력.
      10년 성과가 +5,255%~-68%로 크게 갈려 0단계의 "무지식 종목 선택은 지수를 이기기 어렵다"가 실제로 성립
      (20개 후보 중 13개가 KOSPI +238%에 미달)
- [x] **rates.json** — 한국 정책금리 520주(FRED `IRSTCI01KRM156N`) + 국면 라벨을 실제 경로에서 재계산.
      실제 9개 국면: 2018 인상 → 2020 코로나 인하 → 2020~21 저금리 → 2021~23 대긴축 → 2023~24 고금리 → 2024~ 완화
- [x] **미니 모의투자 구간 재선정** — 기존 구간은 목업 특성 기준이라 전부 무효였음. 실제 구간으로 교체:
      1주차 국면(266/431/218주) · 2주차 적립(266주, 긴축기 — 적립식도 손실 날 수 있음을 보여주는 정직한 구간) ·
      3주차 레버리지(상승장 205주=2020 반등 / 하락장 260주=2022 긴축) · 4주차 자산배분(87주, 코로나 폭락 포함)
- [x] 검증: 3주차 2배·만기일시·하락장 → **26주차 반대매매**, 1주차 긴축기 주식 -16.4% vs 예금 +0.5%,
      4주차 분산 MDD -20.0% vs 주식100% -37%. 전 주차 브라우저 실측, 콘솔 에러 0
- [x] 정기예금 계열 분리(`depositSeries`) — 기존 `cashSeries`(파킹통장, 기준금리-1%p)를 쓰면 저금리기에 이자가 0이 됨
- [x] 면책 문구 갱신 — "학습용 가상 데이터" → 실제 과거 시세이며 종목 추천이 아님을 명시

### 5주차 실명·실데이터 전환 (2026-07-22 완료)
- [x] **DART 오픈API 연동** — 5주차를 실명 기업 + 실제 재무지표·실제 공시로 전환.
      생성 스크립트 `docs/콘텐츠/mock-data/fetch_dart_fundamentals.py` → `src/data/fundamentals.json`.
      · 재무지표: DART 사업보고서(2024). PER=시총/당기순이익, PBR=시총/자본총계, 영업이익률.
        IFRS 표준 계정 ID로 매칭(계정명은 회사마다 달라 실패). 업종 평균 PER=비교군 실제 중앙값.
      · 주가: 지표 공시 후 시점(2025-04) 이후 실제 52주. 뉴스 '신호'=실제 반기보고서 공시일+실적(YoY),
        '소음'=특정 기업 미지목(실제 기업에 지어낸 공시를 붙이지 않기 위함).
      · 실데이터가 교안 주장+반례를 동시 제공: SK하이닉스(저PER+고영업이익률)+406% 1위,
        NAVER(업종 대비 저PER)인데 최저 +7.4%, 삼성전자 매출+5.3%/영업이익-33.4%.
      · **보안**: DART 키는 `.env.local`(gitignore·chmod600)에만, 빌드 타임 스크립트에서만 읽음.
        정적 SPA라 프론트엔드/번들에 키 넣지 않음(VITE_ 금지). `.env.example`만 커밋.
      · 6주차 세금 예시는 가상 손익이라 '종목 A/B/C'로 표기(실명에 없던 손실 씌우지 않음).

## 다음 백로그 (우선순위 순, 미착수)
- [x] ~~실습2 데이터 실측 교체~~ — **2026-07-22 완료**. FRED(`SP500`, `DFF`)에서 2020년 실제
      S&P 500 주간 종가 + 미국 실효 연방기금금리를 받아 교체. 생성 스크립트
      `docs/콘텐츠/mock-data/fetch_sp500_2020.py`(API 키 불필요, 재실행 시 동일 결과).
      실제 기록: 최대 낙폭 **-31.8%**(저점 2,304.92), 연간 **+14.5%**, 금리 1.55%→0.09%.
      기존 체크포인트(12주차 금리 방향 / 34주차 반등 여부) 정답이 실데이터로도 성립함을 실측 확인.
      화면 문구도 "연출된 가상 데이터" → "2020년 실제 데이터 · 출처 FRED"로 교체
## 디자인 개편 + 인포그래픽 + 졸업 리포트 (2026-07-22 완료, main 병합됨)
> ✅ **main 병합 완료**(머지 커밋 `b539be7`, 브랜치 삭제). 롤백 태그 `pre-redesign-2026-07-22`(origin).
- [x] **디자인 시스템 토큰화** — 프리미엄 핀테크 톤(토스/애플/MUJI/Linear). `styles.css` 재정비,
      클래스명·구조 100% 유지(JS 무변경). 8px 간격·radius·타이포 4단계·모션 200ms ease-out 토큰.
      그라디언트·글라스모피즘·과한 그림자 제거 → 보더 우선. WCAG AA(캡션 4.62, 완료뱃지 4.68).
- [x] **2~6주차 강의 인포그래픽** — 인라인 SVG(`infographics.js`), 색은 CSS 변수라 디자인과 한 몸.
      복리곡선/레버리지증폭(±10→±20%)/MDD비대칭/PER도해/손익통산. weeks/index.js에서 카드에 주입.
- [x] **졸업 리포트**(`graduation.js`) — 그때 vs 지금(0단계 -20% → 6가지 기준)/6주가 남긴 것/
      나의 기록/사전진단 재측정. 진입점: 홈·6주차 퀴즈통과. 재측정은 firstScore 보존→"6주 전→지금 +N점".

## 선택 백로그 마무리 (2026-07-22 완료)
- [x] ~~design/premium-fintech → main 병합~~ — 이미 병합 완료(머지 커밋 `b539be7`, 브랜치 삭제).
      기존 미체크는 문서 지연이었음(실제로는 완료 상태) — 정정함.
- [x] **5주차 fundamentals.json 주기적 갱신 스크립트** — `fetch_dart_fundamentals.py`를 `--year`/
      `FISCAL_YEAR` 환경변수로 파라미터화(기본 2024), 주가 조회 종료일을 하드코딩 `2026-07-17` →
      자동 '오늘'(`WINDOW_END`)로 변경. `mock-data/README.md`에 연 1회 갱신 절차·보안 주의 추가.
      **효과**: 다음 사업보고서 공시(매년 3월 말) 후 `--year <직전연도>` 한 줄로 재생성 가능(코드 무수정).
- [x] **파산 임계값 정정(죽은 코드 해소)** — 실데이터 전환 후 단일종목 all-in 최악 매수→최저 홀딩이
      카카오 **20.6%**(=-79.4%)·아모레 22.5%·한전 26.8%라, 기존 `BANKRUPT_RATIO=0.20`(-80%)은
      **8종목 전부 도달 불가한 죽은 코드**였음(목업 -84% 기준으로 잡았던 값이 실데이터에서 무효화).
      → **0.30(-70%)로 하향**. 위 3종에 몰빵+악타이밍 홀딩 시에만 발동, 분산·능동매매엔 안 걸림.
      배너·안내 문구는 `Math.round((1-BANKRUPT_RATIO)*100)`로 자동 반영(하드코딩 없음).
      진짜 '잔고 0' 파산은 여전히 상장폐지 시세 필요(현 8종목 전부 생존 대형주) → 아래 미착수로 유지.
- [x] **진단 오답 → 관련 주차에서 재언급** — 진단 오답 문항을 주차와 매핑(`content.js`
      `weekForDiagnosis`/`missedForWeek`: 카테고리 기준 + PER→5주차·복리→2주차·손실회복→4주차 override).
      해당 주차 강의 **첫 카드**에 '📌 진단에서 놓친 개념' 콜아웃(`.diag-recall`). `appendKids`로
      "null" 렌더 함정 회피. 브라우저 실측: 1주차 2건·5주차 PER override 정확 노출, 콘솔 에러 0.

## 남은 선택 항목 (미착수)
- [ ] 5주차 fundamentals.json 실제 연 1회 재실행 — 스크립트는 준비 완료, 다음 사업보고서 공시 후 실행만.
- [ ] 상장폐지/급락 종목 시세 확보 시 진짜 '잔고 0' 파산 지원 + 임계값 재하향.
- [ ] 진단 오답 재언급을 문항 단위 정밀 매핑으로 확장(현재 카테고리+override 방식) — 필요 시.
